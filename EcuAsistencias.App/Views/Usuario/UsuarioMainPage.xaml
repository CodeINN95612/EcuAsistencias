<Page
    x:Class="EcuAsistencias.App.Views.Usuario.UsuarioMainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EcuAsistencias.App.Views.Usuario"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Usuario" FontSize="60" FontWeight="Bold" Grid.Row="0" Padding="20, 0, 0, 0"/>

        <Grid Grid.Row="1">
            <Frame x:Name="frameContenido"/>
        </Grid>
    </Grid>
</Page>
