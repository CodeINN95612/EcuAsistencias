<Page
    x:Class="EcuAsistencias.App.Views.Usuario.UsuarioLoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EcuAsistencias.App.Views.Usuario"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <RelativePanel HorizontalAlignment="Center" VerticalAlignment="Center" BorderBrush="White" BorderThickness="1">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Grid.Row="0" PlaceholderText="Identificacion" Width="200" Margin="10"/>
            <PasswordBox  PlaceholderText="Contraseña" Grid.Column="0" Grid.Row="1" Width="200"  Margin="10"/>
            <RelativePanel Grid.Column="0" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
                <Button Width="200" Height="30" Content="Iniciar Sesion" Click="Button_Click"/>
            </RelativePanel>
        </Grid>
    </RelativePanel>
</Page>
