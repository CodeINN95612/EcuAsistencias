@model IEnumerable<EcuAsistencias.Models.ReporteriaModels.AsistenciaReporteModel>

@{
    ViewBag.Title = "Asistencia";
}

<h2>Asistencia</h2>

<hr />
@using(Html.BeginForm(null, null, FormMethod.Get))
{
    <input type="date" name="fecha" />
    <input type="submit" value="Buscar" />
}

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Identificacion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Asistio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HoraIngreso)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HorasTrabajadas)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Identificacion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Asistio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HoraIngreso)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HorasTrabajadas)
        </td>
    </tr>
}

</table>
